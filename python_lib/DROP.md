# The Cadmium DROP command

The DROP command removes the specified table from the environment.
The usage is:

```DROP table;```

This is the full syntax, there are no extra parameters - it's just

```DROP table```

See also the [OUTPUT](OUTPUT.md) command.
