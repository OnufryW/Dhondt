RUN FILE "summary_data_by_community.cfg" > 
    FILE "../sejm_election_results/by_district.cfg" INTO table;
OUTPUT TABLES table;
#TRANSFORM ctable WITH
#  if(len(teryt) = 5, "0" + teryt, teryt) AS teryt, 
#  curr() FOR 2:;

#LOAD districts FROM "../district_definitions/prefixes.csv";
#TRANSFORM districts WITH TERC AS TERC, at(2) AS district;

# TODO: Use by_district when we have a parameter-based version of
# specifying which columns to aggregate.
#JOIN districts INTO ctable ON TERC PREFIX teryt AS table;
#AGGREGATE table BY district WITH district AS district, sum(curr()) FOR 4:;
